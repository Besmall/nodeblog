
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>头部</title>

    <!-- Bootstrap core CSS -->
    <link href="/stylesheets/bootstrap/bootstrap.min.css" rel="stylesheet">

  </head>

  <body>

    <div class="container">
            <h2 id="navbar-inverted">文章发布系统</h2>
        <div class="bs-example" data-example-id="inverted-navbar">
            <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-9" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                
                </div>
                
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-9">
                    <ul class="nav navbar-nav">   
                        <li class="<%=locals.isActive('/')%>"><a href="/">首页</a></li>
                        <% if(locals.username){ %>
                        <li class="<%=locals.isActive('/addarticle')%>"><a href="/article/addarticle">发布文章</a></li>
                        <li class="<%=locals.isActive('/article')%>"><a href="/article/">文章列表</a></li>
                        <% } %>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                            <% if(!locals.username){ %>
                                <li class="<%=locals.isActive('/login')%>"><a href="/users/login">登录</a></li>
                                <li class="<%=locals.isActive('/logging')%>"><a href="/users/logging">注册</a></li>
                            <% } %>
                        <% if(locals.username){ %>
                            <li><a href="javascript:;">欢迎回来：<%=locals.username%></a></li>
                            <li><a href="/users/logging" onclick="tuichu()">退出</a></li>
                        <% } %>
                        
                    </ul>
                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
            </nav>
        </div><!-- /example -->
    </div> <!-- /container -->

    <script>
        function tuichu(){
            $.post("/users/signout",function(data){
            if(data.state==1){
                alert("退出成功");
                location.href="/";
            }else{
                alert("退出失败")
            }
            })
        }
        
        </script>
  </body>
</html>
